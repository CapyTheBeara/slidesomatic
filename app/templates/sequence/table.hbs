<div class="table-responsive1 sequences-table scroll">
    <table class='table table-bordered'>
        <thead>
            <tr>
                <th>Time</th>
                <th>Slide</th>
                {{#unless presentationMode}}
                    <th></th>
                {{/unless}}
            </tr>
        </thead>

        <tbody>
        {{#each}}
            <tr {{action 'skipTo' start}}
                {{bind-attr class='isPastSequence:past'}}>
                <td>
                    {{#if presentationMode}}
                        {{format-seconds start false}}
                    {{else}}
                        {{format-seconds start true}}
                    {{/if}}
                </td>
                <td>
                    {{format-slide this}}
                </td>
                {{#unless presentationMode}}
                    <td>
                        <a class='btn btn-danger btn-xs' {{action 'destroy'}}>Remove</a>
                    </td>
                {{/unless}}
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>
