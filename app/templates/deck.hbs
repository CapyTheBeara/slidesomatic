{{#if iframe}}
    {{slide-iframe endpoint = imgEndpoint
                   firstIndex = firstIndex
                   playback = playback}}
{{else}}
    {{slide-image endpoint = imgEndpoint
                  firstIndex = firstIndex
                  playback = playback}}

    {{slide-image endpoint = imgEndpoint
                  firstIndex = firstIndex
                  playback = playback
                  hidden = true}}
{{/if}}
