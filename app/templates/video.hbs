{{popcorn-video id  = 'video-view'
                video = video
                action = 'setTime'
                videoPlayer = player}}

{{#if editMode}}
    {{input-slider action='scrub'}}
{{/if}}
