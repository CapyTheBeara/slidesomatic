{{popcorn-video id  = 'video-view'
                media = video
                action = 'setTime'
                mediaPlayer = player}}

{{#if editMode}}
    {{input-slider action='scrub'}}
{{/if}}
