{{popcorn-video id  = 'video-view'
                src = video.url
                start = video.start
                action = 'setTime'
                videoPlayer = player}}

{{#if editMode}}
    {{input-slider action='scrub'}}
{{/if}}
