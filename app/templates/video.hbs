{{popcorn-video id  = 'video-view'
                src = video.url
                start = video.start
                action = 'setTime'
                videoPlayer = player}}

{{input-slider action='scrub'}}
