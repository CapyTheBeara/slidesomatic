var equal = Em.computed.equal;

export default Ember.ObjectController.extend({
  // passes firstIndex and imgEndpoint to slide-image component

  actions: {
    handleError: function() {  // img error (ie. slide doesn't exist)
// TODO
    }
  }

});
